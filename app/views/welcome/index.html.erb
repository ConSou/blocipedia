<div class="page-header">
<h1><strong>Blocipedia: </strong><i>Social, MarkDown Wikis</i></h1>
</div>

<div class="jumbotron">
  <p>Create and Share MarkDown Wikis</p>
  <% if !user_signed_in? %>
  <%= link_to "Sign Up", new_user_registration_path, :class => 'btn btn-default btn-lg main-signup' %>
  <% end %>
</div>
